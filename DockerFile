# Use an official Node.js runtime as a parent image
FROM node:16-alpine

# Set the working directory in the container
WORKDIR /usr/src/app

# Install required system dependencies (for production environments)
RUN apk add --no-cache bash

# Copy the package.json and package-lock.json to the container
COPY package*.json ./

# Install dependencies (use npm ci for faster builds in CI/CD environments)
RUN npm ci --production

# Copy the rest of the application into the container
COPY . .

# Expose the port that Strapi will be running on
EXPOSE 1337

# Set environment variable for production (optional, but good practice)
ENV NODE_ENV=production

# Run Strapi's start command to launch the application
CMD ["npm", "run", "start"]
